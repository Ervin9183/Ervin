# NESO BSAD Requirement Streamlit App

Streamlit dashboard for NESO Requirement for BSADs data.

## Streamlit Community Cloud

Use this entrypoint when deploying from the repository root:

```text
NESO BSAD Requirement/NESO BSAD Requirement (Streamlit).py
```

The app dependencies are declared in `requirements.txt`.
